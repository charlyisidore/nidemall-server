post:
  tags:
    - address
  summary: Address save
  description: Address save
  operationId: wxAddressSave
  security:
    - ApiKey: []
  requestBody:
    $ref: ../../components/requestBodies/AddressSave.yaml
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../../components/schemas/ApiResponse.yaml
              - type: object
                properties:
                  data:
                    type: integer
