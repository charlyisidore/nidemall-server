# TODO
post:
  tags:
    - auth
  summary: Auth reset TODO
  description: Auth reset TODO
  operationId: wxAuthReset
  responses:
    '200':
      description: Successful operation
      content:
        application/json:
          schema:
            type: object
            allOf:
              - $ref: ../../../components/schemas/ApiResponse.yaml
            required:
              - data
            properties:
              data:
                type: string
    '4XX':
      description: Unexpected error
